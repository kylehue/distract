# DISTRACT

Detecting Inattentiveness in Students Through Remote Assessment using Computer Tracking

### Contributor Notes

-  For development, run `npm run dev` in terminal.
-  To build the project, run `npm run build` in terminal. All output files will be in `release/` directory.
